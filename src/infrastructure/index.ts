/**
 * Infrastructure Layer - Public API
 * Clean Architecture: External services and implementations
 *
 * Note: Mock repositories have been removed in favor of on-chain data
 * via CrowdfundingEscrow contract.
 */

// Services are now accessed directly via lib/contracts/escrow.ts
